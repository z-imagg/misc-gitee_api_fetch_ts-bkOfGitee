```shell
npm install

#编译
npm run build  
#这个构建后的代码会在build/src目录下
 
#运行
npm run start

```

```shell
npm init
npm install   chrome-remote-interface

/app/chrome-linux/chrome --remote-debugging-port=9222
node ./main.js

```